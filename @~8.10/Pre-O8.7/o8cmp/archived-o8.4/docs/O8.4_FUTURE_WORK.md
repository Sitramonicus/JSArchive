# O8.4 Line — Future Work Register

Two sections: **OPEN** (active ideas, nothing built) and **RESOLVED** (decided / shipped / archived — kept for the record so decisions aren't re-litigated).

Naming convention for increments on this line (user decision 2026-09-07): suite `O.8.4.1`, `O.8.4.2`, … (file `O8.4.1.js`); the -Juggler codename was the last of its kind — codenames may return per-increment if desired.

---

# OPEN

## 1. ~~Logging layering & log-lock~~ — DELIVERED: O.8.4.1 (log-lock), O.8.4.2 (credential-order correction + level-0/MemberCount semantics); release-mode strip still OPEN

- **LOG_LEVEL 1 (default, consumer):** started / queue / progress / finished — codename vocabulary (pool words from item 2), not obfuscated.
- **LOG_LEVEL 2 (full diagnostics):** gated behind `GoogleUnlock(passphrase)` (window export), salted SHA-256 digest check via WebCrypto, session-scoped, emission-time gate.
- **LOG_LEVEL 0 = silent except `[MemberCount]`** (user requirement, O.8.4.2): MemberCount emits via its own console path so it survives the all-noop Log module; verified level-0 runs print only the MemberCount line.
- **User-supplied pair (corrected 2026-09-07):** passphrase = `thisisjustfordebuggingwhyinthehelldoyouneedtoknowthecontents` (LONG), salt = `RBaa+,gp#V%w&%Tm|*g8V_{@1C!Q` (SHORT). O8.4.1 shipped the digest over the swapped pair (`3d8c3100…` — stale, kept only as history); O8.4.2 embeds `201f1688cc9ac88301d578cfa6d1e6408dde219cd7b11dda70a184945b917204` = sha256(salt+passphrase). Register stays local-only; artifact carries salt char codes + digest, never plaintext.
- Accepted limitation: a client-side passphrase check is a courtesy lock / noise reducer, not a security wall.
- Release-mode decision pending: strip LOG_LEVEL-2-only strings from release artifacts.

## 2. ~~Phrase-variant lexicon~~ — DELIVERED: O.8.4.2 pools, O.8.4.3 uniform 15-variant pools (user final requirement) — RESOLVED

- O.8.4.2: 20 codename terms → 15-word static alias pools (occurrence *k* → *(7k) mod 15*); 53 phrase keys → variant pools (no fixed count).
- O.8.4.3 (user: "phrases should each have the same amount of aliases just as the terms"): every phrase key now carries exactly **15 variants**, occurrence *j* → slot *j* (multi-occurrence keys cycle *j mod 15*); collision-screened against every O.8.4.2-shipped text (0 overlaps). Completion tells included (`Rounded out:`/`Completed:`/`Checked off:`/`Cleared:`/… at Trophy sites).
- `Google` prefix unchanged; mapping (all pools, all variants, occurrence rules) emitted per suite in the cheat-sheet appendix. Vetoes = one-line pool edit in `lexicon-o843-builder.py` + deterministic regen (seed `4df602e0`).
- O.8.4 line finalized at O.8.4.3 per user.

## 3. Cheat-sheet/register doc maintenance — ACTIVE (emitted per suite)

- O.8.4.1 / O.8.4.2 / O.8.4.3 appendices each emitted at build time (mapping tables + occurrence rules + ops notes). Keep in sync on future suites; round records carry identity (seed/sha).



# O8.5 — line register

1. **O8.5-Shard-1 DELIVERED (2026-09-07)** — `O8.5-Shard-1.js` (suite `O.8.5-Shard-1`, seed `221dfe4d`, sha `dc7771ca…`, record `O8.5-Shard-1_RECORD.md`). Module sharding (SMO) experiment 1: O8.4.3 base split into scope-isolated shards (foundation / engine / decoy) over a tiny contract object (`_0xmod` exporting only `log`, `mc`); V1 run-key neutralized (`Symbol.for("_0x7c1e9f2a")`); full battery green (parity 23/23, consumer/unlocked/L0/L1, unlock 5/5, negative gate, determinism, stress multi 7/7 · midrun 9/9 · srvdone 4/4 · blip 6/6). Seam markers (`SHARD 0/1/2`) retained in artifact for per-shard extraction.
2. **O8.5-Shard-2 DELIVERED (2026-09-08)** — `O8.5-Shard-2.js` (suite `O.8.5-Shard-2`, seed `9a9c42ca`, sha `ff593739…`, record `O8.5-Shard-2_RECORD.md`, pipeline `SHARD2_PIPELINE.md`). Per-call rotating lexicon (`_0xlex.C/P` pickers; no-immediate-repeat; legacy self-name words fixed Doorplate/Pavilion; two consumer runs share 0/12 lines), six balanced `_0xmod` pieces (A/M/N1/E/N2/AUX = 124/310/315/846/296/177 — engine now 41% of file vs 82%), noise inside every shard, decoys wrapped not standalone, seam banners removed, MemberCount shard prominent (cross-shard readout verified: Members 200 | Online 57). Architecture lesson: generator does region surgery, so generate-then-shard (never feed it a pre-sharded template — wrappers merged and scope broke). Engine is one continuous async IIFE: finer engine split infeasible without refactor. Battery green incl. determinism byte-exact, 31-diag unlocked, unlock 5/5, stress midrun 9/9 · multi 7/7 · srvdone 4/4 · blip 6/6.
3. **O8.5-Shard-3 DELIVERED (2026-09-08)** — `O8.5-Shard-3.js` (suite `O.8.5-Shard-3`, seed `2de579ef`, sha `76da265c…`, record `O8.5-Shard-3_RECORD.md`, bundles `release/O8.5-Shard-{2,3}.zip`). User asks: stitcher bundled per instance folder (payload + 6 pieces + stitch-o85.py + README + sums), **no comments in shipped JS moving forward** (`stripc-o85.py`, token-aware; parity tool given code-anchor fallback), plus the forgotten lexicon obfuscation: **entire lexicon encoded at rest** (rot band 0x20–0x7E, KC=47/KP=61, decode on log fire; 0 plaintext words/sentences remain), noise rewritten keyword-free, fresh instance via core inversion (`inv-o85.py`) → generator → lexenc → shardify3 → strip. Battery green incl. determinism, 31-diag unlocked, unlock 5/5, stress 9/7/4/6. Flag-list assessment in `FLAGS_ASSESSMENT.md`.
4. **O8.5-Shard-4 (next)** — first real per-shard obfuscation (obfuscator.io local or CLI 5.6.0, different settings per shard piece, stitch via bundled `stitch-o85.py`, full battery after wrap). Regeneration seed saved: `cores/o85-shard3-enc-core.js` (pipeline: generator on saved core → shardify3 → stripc — do NOT rerun lexenc). Optional: rename Google* facade family (hook/native/progress/tasks) to shorten scanner word surface — cosmetic, cheat-sheet churn. Chop-stitch stays dead; two-pass whole-file is the fallback experiment.
3. **VM obfuscation study** — feasibility + hardening triage + Q&A: `O8.5_VM_OBFUSCATION_FEASIBILITY.md`, `O8.5_HARDENING_TRIAGE.md`. Key config for any obfuscator pass: stringArray rc4 on, moderate flattening, selfDefending/debugProtection/disableConsoleOutput/renameGlobals OFF, console.clear-first re-asserted post-wrap, full battery on the wrapped artifact.

