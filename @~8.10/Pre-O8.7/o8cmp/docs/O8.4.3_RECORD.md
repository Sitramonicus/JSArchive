# O.8.4.3 — Round Record (uniform phrase pools — final O.8.4-line increment)

Date: 2026-09-07. Base: byte-reproduced O8.4.2 chain (`/tmp/o842-unrot.js` → lexicon-o842 → base-o842 → seed `08e2e953` = sha `6358f094…`, verified identical to the shipped O8.4.2.js) → phrase swap layer (lexicon-o843b) → base deltas → generator, fresh seed. Suite `O.8.4.3`. **Finalizes the O.8.4 line** (user: "please finalise O8.4"; next line O8.5 = obfuscator.io VM + segment study).

## Deliverable

| item | value |
|---|---|
| file | `/home/user/o8cmp/O8.4.3.js` |
| suite | `O.8.4.3` |
| seed / instance id | `4df602e0` |
| sha256 | `54907025b61f5fa2e6bd05538b7dbf18e9f71f2215978e55341af66ddbc52e71` |
| size | 58,267 B raw → 17,807 B deflate |
| first log | `[Quest O.8.4.3] started — metamorphic instance 4df602e0.` |
| parity | 23/23 |

## Change (user requirement)

**Uniform aliasing: "Phrases should each have the same amount of aliases just as the terms."** All 53 phrase keys now carry **15 variants each** (matching the 15-word codename pools), composed from curated synonym slots per key — every variant meaning-preserving (same event, same code path, same variables), family markers that must survive (task types, `Alt+Shift+R`, counts, vc-keep-alive hint, error payloads) preserved in every variant. Occurrence *j* (file order) emits slot *j*; multi-occurrence keys cycle *j mod 15*. Pools are deterministic (`random.Random(key)` seeds; full pool list in cheat-sheet appendix) and **collision-screened against every phrase text O.8.4.2 shipped** → 0 overlaps, so the artifact contains no O.8.4.2 phrase text (incl. no `Finished:` — completion family now `Rounded out:`/`Completed:`/`Checked off:`/`Cleared:`/…). Codename pools, log-lock digest `201f1688…`, MemberCount v3, LOG_LEVEL semantics unchanged from O.8.4.2.

**Tooling:** `stress-queue.js` assertions were genericized (colon-terminal `: <Quest>.` completion tells, `(N activity results)` clause, `Error: ` crash negatives, widened progress-beat leads) so queue-mechanics tests survive any future rewording — validated across all four scenarios on this suite.

## Validation battery — ALL PASSED

| check | result |
|---|---|
| `node --check` | syntax OK |
| plaintext parity | 23/23 |
| base provenance | O8.4.2 chain re-run from unrot → byte-identical to delivered sha before 843 layer |
| harness-default consumer | exit 0 · 0 diag · `[Google Medal] Rounded out: Test Quest Zero.` · RELOAD |
| unlocked variant | 31 `[O8-DIAG]` · coverage · `monotonic: true` |
| LOG_LEVEL 1 / LOG_LEVEL 0 | L1 exit 0 · 0 diag; L0 exit 0 · only `[MemberCount]` (+ harness stubs) |
| unlock unit (VM + WebCrypto) | 5/5 PASS (corrected creds, digest `201f1688…`) |
| negative gate | `[Google Bungle] A pocket came up empty — stopping here.` · exit 0 |
| determinism | same template + seed → byte-identical sha `54907025…` |
| stress | multi 7/7 · midrun 9/9 · srvdone 4/4 · blip 6/6 |
| old-alias scan | 0 (Crumb…Fairground absent) |
| cross-version text scan | 0 O.8.4.2 phrase texts in artifact (substring-checked with context) |
| stale-token scan | O.8.4.1/O.8.4.2 tokens absent |

## Notes

- Pipeline (local): `python3 lexicon-o843-builder.py` (pools → `/tmp/o843-phrases.json` + region) → `python3 lexicon-o842.py` (`/tmp/o842-lex.js` — 842 codename pools only) → `python3 lexicon-o843b.py` (position-mapped phrase swap → `/tmp/o843-lex.js`) → `python3 base-o843.py` (`/tmp/o843-base.js`) → `node juggler-metamorph-generator.js --template /tmp/o843-base.js --suite O.8.4.3 --seed 4df602e0`.
- Phrase swaps required re-adding JS quoting (`js_fragment`); occurrence-overlap guard; pools emitted with exact-slot semantics — do not hand-edit emitted texts; edit `lexicon-o843-builder.py` and rebuild.
- Full 53-key × 15-variant mapping + occurrence counts: cheat-sheet O.8.4.3 appendix.
