# O8.12 Operator Tasks (for chat-fail futureproofing — plain text)

Date: 2026-09-15 — O8.12 RIPCORD scoped, O8.11-r3-final frozen (fb32c3a8)

What you (operator) need to do, in order. Everything else is agent-done.

1. **Live-test O8.11 if you haven't** — paste `O8.11-runner.js` (fb32c3a8) in Discord console, check `[Host 8.11] 3a82764d` + `Nothing...` + `Google ledger...` then `glyphfree.pockets` harvest with probes. If green, say "go live O8.11" and agent will `cp -r Archives/packages/O8.11 Working-Stable/O8.11` (rule: Working-Stable only on your explicit say-so).

2. **Give 5 passwords ephemeral** when agent asks for G9 (new order for O8.12): `pwDbg pwRes pwAK pwView pwRcd` (either `pwDbg` or `pwRcd` unlocks main, same level — independent, no dependency) never written. Example call the agent will run: `node Active/O8.6/tools/chore-stress.mjs S15 7 <pwDbg> <pwRes> <pwAK> <pwView> <pwRcd>` (and also S8/S9/S10/S12 now need main within 60s). You can give fragments for grep sweeps: agent will ask for distinct fragment per password via ask_user, then `grep -rl "<fragment>" --exclude-dir=node_modules --exclude-dir=.git .` must be 0 hits.

3. **Expect 60s window + 会員 levels** — after paste, call `await window.GoogleUblock("ripcord")` **or** `await window.GoogleUblock(<pwDbg>)` within 60s (either works, independent flags — no else-if). Levels: `会員=0`→0 regardless; `会員=1 + rcd/dbg`→1; `会員=2 + rcd`→1; `会員=2 + dbg`→2 (only dbg gives 2). Without either within 60s, everything deflects (`queue pool refresh blocked — rcd required`). `pwView/pwAK/pwRes` work with either at level 1, but L2 view needs `会員=2+dbg`.

4. **Do not push to GitHub yet** — node_modules is gitignored (245MB, reinstall `cd Active/engines && npm install`). Credentials still in git history (commits 20d1bab etc) — history rewrite needed before first push, or rotate passphrase (agent won't push without your explicit).

5. **Literal hygiene** — `Active/` will contain **no** literal `"ripcord"` / long debug string after G7 (hashed only, `grep -r ripcord` 0). This doc can mention it, code cannot.

6. **When O8.12 build is done**, repeat live-test on `O8.12-runner.js` (will be `fb...` new) — check: no rcd/dbg → deflection, late (>60s) → deflection, `会員0+rcd/dbg→0`, `1+rcd/dbg→1`, `2+rcd→1`, `2+dbg→2`, garbled platform rcd → decoy-over-decoy. Say "go live O8.12" to freeze to Working-Stable.

How we got here vs what's next (plain):
- Worked: O8.11 equal-tunnel + mounded pockets + heavy Zoom 100.9KB + 660 cover + G8 42/42 + S2/S4/S6 cohesive (see EVAL-REVIEW and D-TRACK-NOTE — D is the honey track).
- Failed/why: Quest loop still pinpointed (eval found Discord wins via execution semantics) — needs deflection.
- Next/avenues: RIPCORD (O8.12) adds `pwRcd` gate (60s) in shard-u (not e), pocket logs recycle cheatsheet under Google prefix, 5 calls per platform uniformly, garbled per-platform fakes with decoy-over-decoy, no auth-token calls. Limitations: O8.11 battery still 25/25, but rcd adds ~10KB and may need stego 660→680 if util >99.5%.

Keep this file — if chat dies, new agent reads HANDOFF + O8.12-PLAN + this.
