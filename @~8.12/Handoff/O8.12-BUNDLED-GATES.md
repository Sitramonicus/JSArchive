# O8.12 Bundled Gates — execution order (per your "bundle the gates" note)

Date: 2026-09-15 — O8.11 FROZEN, O8.12 RIPCORD (rcd/dbg + 会員 levels) — this file bundles the 10 detailed gates from O8.12-PLAN.md into 3 runnable bundles so we can move fast and keep Handoff updates small.

## Upgrade rule you flagged (important)

* **If `var 会員 = 2` and you run with `pwRcd` (`"ripcord"` → level 1), you may later *upgrade* to `pwDbg` (`"thisisjustfordebugging..."` → level 2) without a reload.** This was already in the old logic before `rcd` and must stay.
* How we keep it working with the new OR gate: `level` is recomputed every call (`level = 会員==0?0 : 会員==2 && dbgOK ?2 : mainOK?1:0`, where `mainOK=dbgOK||rcdOK`). So `rcdOK=true → level 1`; later `dbgOK=true → level 2` automatically (no extra branch, no `else`). Complication you warned about (introducing `rcd` breaks upgrade) is avoided because we use **independent flags, no `else if`**, and `pwView/pwAK/pwRes` check `level>=1` (either key at level 1), so upgrading doesn't lose their side-effects. `60s` window only gates the *first* `mainOK`; upgrade after `mainOK` is allowed even beyond 60s (so you can `rcd` quickly then `dbg` later). If neither within 60s → `DEFLECTION` forever until reload.

## Bundled execution (3 bundles + supplementaries)

**Bundle 1 — FIRST BUNDLE (we start now, per your "let's continue with the first bundle"):**
* G1 `BUILD-SEED respin + rotation o811→o812` + G2 `pocket shards plain + shard-u gate` + shard-e 5-call (no gate)
* Tasks (plain, no obfuscation):
  - `node Active/O8.6/tools/respin-seed.mjs` → new 8-hex, `seed-lib` derives
  - `node Active/O8.6/oto/scripts/rotate-ioc.mjs o812 --apply --version=8.12` → new `instance/symbol/codenames/cycleMs`
  - Edit `shard-u.js` → add `dbgOK/rcdOK` OR gate + `会員`-gated `level` (0/1/2) + 60s timer + upgrade, **no literal `"ripcord"`** (G7 hash only), deflection via `SW_BAD` gradual
  - Edit `shard-e.js` → 5 Discord calls (uniform)
  - Edit `shard-p-*.js` ×4 → each 5 calls (Teams without `getAuthToken`, using `dialog.url.submit`) + 5 recycled `Google`-cheatsheet logs (deterministic, no per-call random) + garbled per-platform `rcd` decoy-over-decoy
  - `node --check` all 11 shards, `stitch-o85.py` still 11 pieces, `grep -r ripcord Active/` 0
* Gate: `stitch` boots, `battery 25/25` still green, `grep -c` 5 per platform uniform.

**Bundle 2 — Strings + Obfuscation:**
* G3 `G7 strings` (`obf-strings-g7.js` → 11 tags `~30` each, census `~1020`) + G4 `OTO mounded` (`obf-minify-family 55 OK`, `v2` heavy Zoom 103KB + mound `u` so `rcd` not outlier, `v4` canon etc)
* Gate: `g7 self-test round-trips`, `battery 25/25`, `0 adjacent dups`.

**Bundle 3 — Package + Stego + Proof:**
* G5 `Stitch + Top-1 5k + G8 repin` (≈1840KB, `EXPECTED_V2_M` repin if `e/u` changed) + G6 `Stego O8.12` (clone `build-stego11p→build-stego12`, `660` 99% or bump to `680`, `DSEED`/`EXPECT` recalibrate, `PSNR ≥31`) + G7 `Tiers/Matrix rcd-aware` (`T2 no rcd/dbg→deflection`, `会員0→0`, `1+rcd/dbg→1`, `2+rcd→1`, `2+dbg→2`, garbled→decoy-over-decoy) + G8 `Password stress` (`chore-stress` 5-slot `pwDbg pwRes pwAK pwView pwRcd`, `S15` upgrade scenarios + `S8/S9/S10/S12` still PASS)
* Gate: `carrier 12/12`, `tiers 41→44/44`, `matrix 24→27/27`, `trap-fp/tube ALL PASS`, `grep -rl fragment` 0.

**Supplementaries (bundled, none new to introduce for O8.12 — carry over):**
* `S1` JSO-seed rotation (already via G1), `S2` `document.cookie 0`, `S3` progress-gated `D-MAX`, `S4` loader lite (`13260B`), `S5` cover `660→maybe680` (if needed for +10KB rcd/logs), `S6` vault `2220` — all stay green via same checks as Bundle 3. No new supplementaries to introduce this generation unless you want one (we can add `S7 rcd-logs` as explicit if you prefer, but it's covered in G7).

**Next:** I will run **Bundle 1** now (respin + rotation + plain shard edits). After Bundle 1 green, we proceed to Bundle 2, then 3. `Working-Stable/O8.12` only on your explicit "go live" after live-test.

---
Checks: `会员` spec per your note (0→0, 1+either→1, 2+rcd→1, 2+dbg→2, upgrade allowed), `ripcord`/`thisisjust...` literals never in `Active/` after G7, `getAuthToken` removed from Teams (replaced with `dialog.url.submit`), `5×5` uniform, `rcd` in `shard-u` not `e`.
