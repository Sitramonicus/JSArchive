# O8.2-Juggler-7 — Colleague Review Triage (2026-09-07)

Verdicts against the actual source of `O8.2-Juggler-7.js` (instance `30cae241`). Summary: **2 confirmed bugs (diag-only), 1 confirmed decoy defect, 1 fair gap, 2 partial/context-dependent, 4 disagree or already-mitigated.**

| # | Point | Verdict | Evidence / action |
|---|---|---|---|
| 1 | `console.clear()` counterproductive | **Disagree** (user-requested) | Added at user request (QoL: keeps pastes/context small). Runs before any quest log exists; nothing of ours is erased. Debugging aid is the headless harnesses, not the live console. No change. |
| 2 | Sanity regex too restrictive as proof | **Agree as heuristic** | It IS a warning heuristic, not proof — by design. All 23 current plaintexts lie inside the charset (verified 23/23), so no false positives today. **Action:** generator will assert charset at encode time so any future string outside the set fails the build, not a live run. |
| 3 | Decoder harder to validate | **Agree (accepted cost); evidence exists** | Metamorphism is the point of the line. Mitigation battery: per-instance VM parity check (23/23), 6-seed fuzz, determinism, harness default/hidden, negative gate. Live indirect checks the reviewer asks for all pass in every run: capture works (⇒ `_0xq0` correct), native bridge (`_0xq1`), routes resolve (server-accepted progress ⇒ `_0xq2..q4`), 7/7 pockets (⇒ all ten method names). |
| 4 | MemberCount obfuscation + dead bookkeeping | **Disagree (intentional), partially fair** | Red-herring region is user-mandated (they want it kept and upgraded); inert scaffolding is deliberate for plausibility. Behavior verified byte-identical (3 isolated branch tests). Decode correctness is static (fixed key/arrays, unit-tested) — "silent no-data" cannot occur from mis-decoding, only from a genuinely empty store, which is the normal case logged each run. |
| 5 | URL guard misses nested shapes; coerce-to-empty | **Legit limit, no live impact yet** | `GoogleId` unwraps only `{id/applicationId/questId}`. Call sites today always pass pre-extracted strings (quest `id`, `v.app`), so nested wrappers never arrive. Still worth deepening (`application.id`, `quest.id`, …) + deciding unresolvable behavior (see O8.3 scope question). |
| 6 | Timestamp `monotonic` always false | **CONFIRMED BUG (diag-only)** | Line 554–558: `lastTs = ts` runs before `monotonic: ts > lastTs`. Timestamps themselves are already monotonic by construction (`max(lastTs + 0.01, rawTs)`); only the log field is wrong. Fix: compare against previous value. |
| 7 | Cleanup logs before `finishTask()` | **CONFIRMED (diag-only)** | Desktop cleanup (l.606–607) and stream cleanup (l.661–662) log `activeTaskCount` while the finishing task is still in the set → count reads 1. Not a leak (cleanup is idempotent; finishTask removes). Fix: log after finish. |
| 8 | Auxiliary region unnecessary / risky | **Disagree (user-mandated); one real defect** | Decoy noise block is a user-approved secrecy layer, generator-rolled per instance, own IIFE, never touches real logic. Reviewer is right that one guard is NOT never-true: `(1<<32)===1` is `true` in JS (shift-count masks mod 32) — verified at runtime — so that branch executes a benign pure function. **Action:** generator fix — make that guard genuinely never-true. Parse cost is ~7 KB on a 53 KB file (one-time). Offer `--bare` flag for clean review builds. |
| 9 | Config claims features never logged | **Fair** | `randomizedIdentityFallback`/`randomizedActivityKey` appear only in the config summary; the behaviors exist inline (e.g., randomized fallback PID at task start) but have no named runtime log, and no live log has yet exercised the stream/activity paths. **Action (optional O8.3):** targeted level-2 usage logs + headless coverage for those paths. |
| 10 | Core intact | Agree | Push-return capture, seven-pocket discovery, forcer, validation, guards all preserved (diff-scope checks). |

## Decisions carried into O8.3 scope (proposed)

1. Fix #6 (timestamp log), #7 (cleanup log order) — both one-liners in the template base.
2. Generator: never-true decoy guard fix; charset assertion on encode; `--bare` review profile (no decoys/scaffolding).
3. URL guard: nested-depth unwrap; fail-closed vs coerce decision pending (question asked).
4. Queue re-check on finish (user's O8.3 feature) — see design note below.
5. Optional: #9 usage logs at randomized-feature use sites.
