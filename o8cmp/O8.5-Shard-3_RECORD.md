# O8.5-Shard-3 — Round Record

Suite: `O.8.5-Shard-3` · seed `2de579ef`
SHA-256: `76da265ca051b1e8e7a999d4fd8263bcc073e87a6dcd4f3292bba249ca03922c`
File: `/home/user/o8cmp/O8.5-Shard-3.js` · 2,055 lines · 190,736 B raw · 36,109 B deflate
Release bundle: `/home/user/o8cmp/release/O8.5-Shard-3.zip` (artifact + 6 comment-free
pieces + stitch-o85.py + README + SHA256SUMS)

Reproduce byte-exact:
```
python3 inv-o85.py                          # Shard-2 pieces -> single-scope core
node juggler-metamorph-generator.js --template /tmp/o85core-plain.js \
     --suite O.8.5-Shard-3 --seed 2de579ef --out /tmp/o85c3.js
python3 lexenc-o85.py                       # encode lexicon at rest (/tmp/o85e3.js)
cp /tmp/o85e3.js /tmp/o85core.js && python3 shardify3-o85.py   # -> O8.5-Shard-3.js
python3 stripc-o85.py O8.5-Shard-3.js O8.5-Shard-3.js          # strip comments
```
Unstripped pre-shard core preserved at `cores/o85-shard3-enc-core.js` for the next regen.

## What changed (user's three asks + the forgotten item)

1. **Stitcher bundled per instance.** Every release folder now ships the payload,
   the six per-piece files, `stitch-o85.py`, a README with per-piece obfuscator
   settings + stitch order, and SHA256SUMS (zips in `release/`). Same for Shard-2
   (with comment-free pieces so the new standard applies to the file the user
   actually pasted).
2. **No comments in shipped JS — moving forward.** `stripc-o85.py` (token-aware:
   strings, templates with ${} nesting, regex literals) strips every `//` and
   `/* */` from the artifact and from all six pieces. Verified 0 occurrences of
   `//`, `/*`, `*/`, box-drawing, and every former region banner. The parity tool
   gained a code-anchor fallback (`const _0xJRe = [` … `Log.diag("Juggler coverage"`
   line) so it still works on comment-free files (all three suites pass).
3. **The "forgotten" item — log lexicon obfuscated (encoded at rest).** The real
   fingerprint the reviewer's list exposed was plaintext lexicon mass: the central
   300-word table and every phrase-variant sentence sat in the file. Now ALL of it
   is Caesar-rotated over the printable band 0x20–0x7E (keys KC=47 words, KP=61
   phrases; non-ASCII like em-dash kept verbatim and skipped by the decoder) and
   only decoded in memory when a log fires. Template literals keep their `${expr}`
   parts verbatim (they carry runner-local state) with only the static text
   encoded as `${_0xlex.d("…")}` segments. Verified: 0 occurrences of any pool
   word or sentence fragment ("Bungle", "Kitbag", "Valise", "Rounded out:",
   "Pockets checked:", …) in the artifact. Console output is byte-identical in
   behavior: English words, per-call random draw within term/phrase families,
   two runs share 0/12 log lines.
4. **Noise rewritten keyword-free** (no .set/.get/.push/.has/.mark/.forEach, no
   Map/Set/class) so naive word-scanners stop lighting up on the camouflage; the
   engine head now carries noise too (was a clean-band tell in Shard-2).
5. Fresh metamorphic instance (new decode tables / decoys / instance id), built
   via `inv-o85.py` (Shard-2 pieces → single-scope core, exact: 46/94/718/22 body
   lines match the original slices) → generator → encode → shardify → strip.

## Battery — all green on the shipped file

- Default consumer ×2: exit 0, 0 diag, `<<RELOAD CALLED>>`, 0/12 identical lines.
- Parity 23/23 (code-anchor path; Shard-1 & Shard-2 still pass via comment path).
- Unlocked variant: exit 0, exactly 31 `[O8-DIAG]`, monotonic timestamps.
- LOG_LEVEL 1: 0 diag + RELOAD. LOG_LEVEL 0: MemberCount-only output.
- unlock-unit 5/5 (digest `201f1688…` unchanged; salted; passphrase never ships).
- MemberCount cross-shard with guild stores: `[MemberCount] Members: 200 | Online: 57`.
- Negative gate (prefix mangled): exit 0. Duplicate-paste: guard warns once.
- Stress: midrun 9/9 · multi 7/7 · srvdone 4/4 · blip 6/6.
- Determinism: same chain → byte-identical sha (checked post-strip).
- Residue: `O.8.5-Shard-2`, `9a9c42ca`, `277ae39b`, `O8.4` all zero; digest present.
- Comment/lexicon scans: zero (see above). `console.clear()` first.

## Decoder reference (record-only; not for the cheat sheet)

dec = rot over [0x20,0x7E]: c' = 0x20 + (((c-0x20-k) % 0x5F) + 0x5F) % 0x5F for c<0x80,
else unchanged. KC=47 (codename words), KP=61 (phrase texts). Sites call
`_0xlex.C(i)` / `_0xlex.P(i, arr)` unchanged; phrase arrays decode per element at
call time via `_0xlex.d("…")`.

## Flag-list disposition (user's scanner output on Shard-2)

See `FLAGS_ASSESSMENT.md`. In short: comment hits → removed; lexicon/pool/sentence
hits → encoded away (the dominant mass); ~40 noise hits on Set/Get/Push/Mark/Map/
forEach → noise redesigned token-free; remaining hits are real engine identifiers
(Quest/Task/Progress/PID/cmdLine/dispatch/…) that mirror the actual Discord
surface and standard-library API names — low risk, and only renameable at
ops/cheat-sheet cost; the log-lock/branding strings ("Quest"/"Unlock") are the
product's own console language and stay.

## Superseded / kept

- Shard-1, Shard-2, O8.4.3 files untouched (frozen). `juggler-instance-check.js`
  updated (additive fallback) — old artifacts still pass.
- `cores/o85-shard3-enc-core.js` is the regeneration seed for the next round
  (fresh instance: generator + encode is no-op on encoded text? No — encode pass
  must NOT be rerun; pipeline for Shard-4 = generator on the saved core → shardify3
  → strip).
