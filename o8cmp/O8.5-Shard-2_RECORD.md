# O8.5-Shard-2 — Round Record

Suite: `O.8.5-Shard-2` · seed `9a9c42ca`
SHA-256: `ff5937392707fbc2662bb81bc359168e17300d0513434c77fe6adf4d04250e6d`
File: `/home/user/o8cmp/O8.5-Shard-2.js` · 2,078 lines · 174,071 B raw

Reproduce byte-exact:
```
python3 dyn-lex-o85.py \
  && node juggler-metamorph-generator.js --template /tmp/o85dyn-base.js \
       --suite O.8.5-Shard-2 --seed 9a9c42ca --out /tmp/o85core.js \
  && python3 shardify-o85.py
```
(Verified: same-seed rebuild is byte-identical to the shipped sha.)

## What Shard-2 adds over Shard-1 (user's redefinition)

1. **Per-call rotating lexicon.** All 58 codename sites call `_0xlex.C(term)` and all 60
   phrase sites call `_0xlex.P(key, [15 variants…])`; the picker is random with a
   no-immediate-repeat guard, one draw per log call. Empirically two back-to-back
   consumer runs share **0 of 12** `[Google …]` lines, and every word stays inside its
   correct term family (Satchel pool → Valise/Knapsack/Haversack…).
   Pools: the original 20 codename pools ×15 (incl. legacy collisions fixed:
   `Doormat`→`Doorplate`, `Arcade`→`Pavilion` so a pool can never emit its own term
   name) and the full 53 phrase pools ×15 plus the pockets-prefix pool — all intact.
   Parity harness still decodes 23/23 because the manifest constants are untouched;
   pool tables ship in the clear per site (they compress: deflate ≈ 26 KB).
   Coherence for observers: the cheat-sheet appendix now documents pools + rule
   (per-call draw), not fixed site→word tables.

2. **No size tell + no standalone decoy shard.** Six scope-isolated `_0xmod` wrappers
   (A foundation 124 / M MemberCount+lexicon 310 / N1 camo 315 / E engine 846 /
   N2 camo 296 / AUX decoys+noise 177). The engine is one continuous async IIFE
   (lines 184–868 in the template) and cannot be sliced without breaking closures,
   so it remains the largest piece — but it now holds ~41% of the file (was 82%),
   every piece carries inert noise machinery (98 noise IIFEs total), decoys are
   wrapped inside the AUX module, and all seam banner comments (`SHARD`, `══`,
   module-sharding seam) are gone.

3. **Noise inside every shard.** Deterministic inert IIFEs (map/hash/class/loop
   shapes, no I/O, dead guards) are injected into A, M, E (head+tail) and around
   the decoy block; N1/N2 are pure camo modules with decorative `_0xmod` exports.

4. **MemberCount shines.** Its wrapper is the second-largest piece (310 lines: the
   95-line red-herring readout region + lexicon helper + 205 lines of glow noise).
   Cross-shard call verified live: with guild-shaped stores mounted, the engine
   wrapper's readout call through `_0xmod.mc` prints
   `[MemberCount] Members: 200 | Online: 57` (123+77 / 45+12 aggregated).
   Still read-only, no hooks, no network, no subscriptions.

## Architecture lesson (recorded in SHARD2_PIPELINE.md)

The generator does region surgery (decode cut/re-emit, decoy strip/re-emit, head
banner rewrite). Feeding it a pre-sharded template silently merged wrappers and
broke scope (observed `Log is not defined` in engine). Canonical flow is therefore
**generate first (single scope), shard the instance afterwards** at top-level
statement seams; the only safe seams are depth-1 boundaries because the engine is
one closure. Legacy flags & guard (`_0xrunKey` early return) ride inside the engine
wrapper so duplicate-paste protection still aborts the whole run.

## Battery — all green on the shipped file

- Default consumer harness ×2: exit 0, 0 `[O8-DIAG]`, `<<RELOAD CALLED>>`, 0/12 identical lines.
- Parity: 23/23 decoded constants identical to manifest.
- Unlocked variant (`_0xopen=true`): exit 0, exactly 31 `[O8-DIAG]` (baseline now reads
  "O8.5 core — …"), timestamp samples monotonic, 5 decimals.
- Level 1 variant (`LOG_LEVEL=1`): 0 diag, RELOAD. Level 0: MemberCount-only output.
- unlock-unit: 5/5 PASS (digest `201f1688…` unchanged, salted, never the passphrase).
- Negative gate (prefix `[Google ` → `[Bungle `): exit 0, RELOAD.
- Stress suite: midrun 9/9 · multi 7/7 · srvdone 4/4 · blip 6/6 (identical counts to Shard-1;
  two pinned-wording waits relaxed to wording-agnostic forms for dynamic words).
- Determinism: same template+seed rebuilds byte-identical (twice, before/after final tweaks).
- Duplicate-paste: second paste in same window → "An O.8 run is already active" warn; run continues.
- Residue scans: `O.8.4.1`/`277ae39b`/`O.8.5-Shard-1`/`O8.4`/`[O7-DIAG]` all zero; no `SHARD`/`══` seams.
- `console.clear()` first; suite token `O.8.5-Shard-2` in banner; `Google` prefix mechanics unchanged.

## Per-piece obfuscation handoff (user has no node_modules)

Per-piece files (each standalone-valid, wrapper + own aliases, `node --check` OK):
`shard-out2/shard-a.js` · `shard-m.js` · `shard-n1.js` · `shard-e.js` · `shard-n2.js` · `shard-aux.js`
(124 / 310 / 315 / 846 / 296 / 177 lines).

Manual: obfuscate each piece separately at obfuscator.io (per-piece settings as in the
Shard-1 table; string-array RC4 heavy on the E piece; keep selfDefending /
debugProtection / disableConsoleOutput / renameGlobals / unicode / split OFF), then
`python3 stitch-o85.py shard-out2/shard-a-out.js shard-out2/shard-m-out.js … O8.5-Shard-2-obf.js`
(stitch-o85.py takes any ordered piece list). CLI fallback: `npm view javascript-obfuscator`
→ 5.6.0 installable locally for reproducible seeded runs.

## Superseded intermediates (do not reuse)

`shard2-o85.py` / `shard2-o85-base.js` were the abandoned pre-sharding-template
experiment; the generator mangled that layout. Canonical pipeline = the 4-step block above.
