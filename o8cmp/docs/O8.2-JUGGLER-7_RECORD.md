# O.8.2-Juggler-7 — Round Record (decoys + MemberCount red herring + prefix fix)

Date: 2026-09-07. Base template: `O8.2-Juggler-6.js` (delivered J-6, unchanged). Subsystems touched: **head/logging cosmetics, MemberCount region (encapsulated, behavior-identical), new decoy noise block, fresh decode instance**.

## Deliverable

| item | value |
|---|---|
| file | `/home/user/o8cmp/O8.2-Juggler-7.js` |
| suite | `O.8.2-Juggler-7` |
| seed / instance id | `30cae241` |
| sha256 | `7b095e1db5a870f056b77b9af435d0ce2047178188756bc8826ce3b2853a7a82` |
| size | 53,158 B raw → 16,014 B deflate (J-6 was 44,669 B; +8.5 KB = decoys ~7.1 KB + MC scaffolding/encoding) |
| first log | `[Quest O.8.2-Juggler-7] started — metamorphic instance 30cae241.` |

## Changes in this round

1. **Decoy noise block** (user-approved, full Tier A+B+C, generator-driven): one contiguous `generated auxiliary region` — 3 fake string tables (~200 entries), 7 dead pure functions, 3 lookalike classes with inheritance, never-true opaque guards, and one harmless lifeline call (pure math) so the block has surface life. Per-instance random names/content; wrapped in its own nested IIFE so nothing can touch real scope; nothing in it touches webpack/module-store/network. The generator **strips any previous block and emits a fresh one every round** (idempotence proven by J-8 dry run: exactly one marker pair, parity 23/23, syntax OK).
2. **MemberCount red-herring region** (user decision: encapsulate + inert scaffolding; never remove): definition now sits in a clearly-marked `client-state readout region`; its 13 literal strings (property probes, log lines) are encoded with a local XOR key + local decoder **inside the region** — zero plaintext `memberCount`/`member statistics` remains greppable in the file. Inert scaffolding added (window table, seen-set bookkeeping, frame/rank helpers — all dead). Runtime behavior **byte-identical**: three isolated branch tests PASS (no-data line, `Members: 5 | Online: 2` suffix path, members-Map path). Removal = delete the marked region + the one `// readout call` block (both clearly marked).
3. **Diagnostic prefix rename** (user catch): logger tag `[O7-DIAG]` → `[O8-DIAG]`. Zero `O7-DIAG` occurrences remain in the file. (Historical docs keep the old spelling.)
4. **Fresh instance**: method order add/xor/tbl/rot; usage add 10 / xor 7 / rot 4 / tbl 2; pool 8 strings / 153 B; 23/23 decode. (J-4-era coverage docs referenced `[O7-DIAG]`; live shape is now `[O8-DIAG] Juggler decode {…}` / `[O8-DIAG] Juggler coverage {…}`.)

## Validation battery — ALL PASSED

| check | result |
|---|---|
| `node --check` | syntax OK |
| plaintext parity (`juggler-instance-check.js`) | 23/23 identical to manifest |
| harness-default | exit 0 · version-first log · 7/7 pockets · `Polished: Test Quest Zero.` · `<<RELOAD CALLED>>` · decoys silent |
| harness-hidden | exit 0 · reload path |
| negative harness (no compass) | `Satchel's missing pockets — heading home.` · exit 0 |
| LOG_LEVEL=1 | version first · zero `O8-DIAG` · quest completes |
| determinism | `--seed 30cae241` → byte-identical (same sha256) |
| J-8 dry run (template = J-7) | decoy strip + fresh insert, exactly 1 marker pair, parity 23/23 |
| MC branch unit tests (isolated VM) | CASE1/2/3 PASS — output identical to pre-encapsulation lines |
| env residue scan | clean |

## Notes / known surface

- The runtime `[MemberCount] …` log line still prints on every run (it is the herring showing itself; user wants it kept). If it should ever be silenced/rebranded, it is a one-line change inside the marked region — their call, no urgency.
- `undefined` console echo, Discord-internal overlay lines, mid-run capture habit: unchanged guidance — capture through `Polished:` for completion proof.
