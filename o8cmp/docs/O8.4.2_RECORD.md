# O.8.4.2 — Round Record (lexicon v2: alias pools + phrase pools + alive MemberCount + corrected log-lock)

Date: 2026-09-07. Base: `/tmp/o842-unrot.js` (O8.4.1 reverse-rotated to literal template) → lexicon pass → base deltas → generator → fresh instance. Suite `O.8.4.2`.

## Deliverable

| item | value |
|---|---|
| file | `/home/user/o8cmp/O8.4.2.js` |
| suite | `O.8.4.2` |
| seed / instance id | `08e2e953` |
| sha256 | `6358f094eef1c47be285e6ae7602f7e9d4ae7ae2650417b194a7d93c770de62b` |
| size | 58,189 B raw → 17,637 B deflate |
| first log | `[Quest O.8.4.2] started — metamorphic instance 08e2e953.` |
| parity | 23/23 |

## Changes (user scope answers 2026-09-07)

1. **Alias pools (user: “~15 per term after all”).** Each of the 20 semantic terms now maps to a curated **static 15-word pool**; occurrence *k* in file order emits pool word *(7k) mod 15* (static per suite — no per-build/instance churn; rule documented so it stays debuggable). `Google` prefix unchanged (user). Old O.8.4.1 one-shot aliases (Crumb…Fairground) are gone from the artifact (byte scan 0). Pool-words were collision-checked against the whole file before use; in-suite collisions (Trail, Rug) were replaced (→ Trek, Doormat) in the pools.
2. **Phrase pools incl. completion tells (user: yes, even the tells).** ~53 code-phrase keys now draw from variant pools (occurrence *j* → variants *j mod len*); pools deliberately do **not** contain the O.8.4.1 literals, so old phrase texts are gone from the artifact (scan 0). Single-occurrence phrases ship variants[0]. This includes `Polished:` → `Finished:` / `Spruced up:` / `Done and dusted:` cycle, gate line, shelf/armed/orchard lines, frac/sand/tile/drawn-open/etc. Each key is mapped in the cheat-sheet O.8.4.2 appendix (full pool + meaning + occurrence count).
3. **Log-lock credential order corrected (user correction).** Password is the long string (`thisisjustfordebugging…contents`), salt the short one (`RBaa+,gp#V%w&%Tm|*g8V_{@1C!Q`). O8.4.1 shipped digest `3d8c3100…` over the swapped pair — O8.4.2 embeds `201f1688cc9ac88301d578cfa6d1e6408dde219cd7b11dda70a184945b917204` = sha256(salt+passphrase), verified independently (Python + sha256sum). Salt stored as char codes; plaintext never ships.
4. **LOG_LEVEL 0 = silent except MemberCount (user requirement).** MemberCount now prints through its own `console.debug` emitter (`_0xemit`) instead of `Log.info`, so it survives the all-noop Log at level 0. Verified: level-0 run output contains exactly the `[MemberCount]` line and nothing else (besides the harness's own sim/watchdog lines).
5. **MemberCount v3 “alive + flavor” (user choice).** Adds: multi-source readout (memberCount / approximateMemberCount / member_count / members-map fallback), online/presence counts, result cache + 60 s staleness, and an occasional `[MemberCount] Still watching N servers — nothing unusual.` flavor line only after a successful readout with ≥60 s idle. Read-only over existing local handles; **no new network, hooks, or subscriptions** (constraint). Summary call site added once after the active-tasks drain.
6. **Validation tooling made lexicon-aware.** `stress-queue.js` hard-coded O8.2-era phrase shapes (`Polished:`, `pinned to the board`, `All polished`, …); updated 24 assertions to pool-family alternations + quest-name anchors so queue mechanics stay under test as cosmetic texts vary.

## Validation battery — ALL PASSED

| check | result |
|---|---|
| `node --check` | syntax OK |
| plaintext parity | 23/23 |
| harness-default consumer (diag locked) | exit 0 · `[Google Medal] Finished: Test Quest Zero.` · `<<RELOAD CALLED>>` · 0 `[O8-DIAG]` lines |
| unlocked variant (sed `_0xopen=true`) | 31 `[O8-DIAG]` lines · Juggler coverage · `monotonic: true` |
| LOG_LEVEL 1 variant | exit 0 · 0 diag |
| LOG_LEVEL 0 variant (timer harness) | exit 0 · only `[MemberCount]` visible · quest/say lines absent |
| unlock unit (VM + WebCrypto, corrected creds) | 5/5 PASS: exported · wrong rejected + warn · right accepted + info |
| negative gate (harness-nopocket) | exit 0 · `[Google Bungle] Missing pockets — heading home.` |
| determinism | same template + seed → byte-identical sha256 `6358f094…` |
| stress midrun | 9/9 |
| stress blip | 6/6 |
| old-codename scan | 20/20 O.8.4.1 aliases absent from artifact |
| stale-token scan | `O.8.4.1`/`O8.4.1`/`277ae39b`/`2501a70a…`/`3d8c3100…` all absent |

## Notes

- Phrase/alias mapping (incl. every variant and occurrence rule) is in the cheat-sheet O.8.4.2 appendix; vetoes are one-line pool edits + regen (deterministic seed).
- Live capture: default runs are consumer-level; diagnostics need `await GoogleUnlock("<passphrase>")` (register holds the pair). At level 0 nothing but MemberCount prints.
- Gate site codename: Puddle occurrence *k=2* → pool index *(7·2) mod 15 = 14* → `Bungle` (so the negative gate line is predictable per suite).
- Build pipeline (local): `python3 lexicon-o842.py` (lex output `/tmp/o842-lex.js`) → `python3 base-o842.py` (writes `/tmp/o842-base.js`) → `node juggler-metamorph-generator.js --template /tmp/o842-base.js --suite O.8.4.2 --seed 08e2e953`.
